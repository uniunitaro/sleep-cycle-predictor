import { Meta, StoryObj } from '@storybook/react'
import AuthFormCard from './AuthFormCard'

export default { component: AuthFormCard } satisfies Meta<typeof AuthFormCard>

export const Default: StoryObj<typeof AuthFormCard> = {}
