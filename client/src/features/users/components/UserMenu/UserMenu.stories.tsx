import { Meta, StoryObj } from '@storybook/react'
import UserMenu from './UserMenu'

export default { component: UserMenu } satisfies Meta<typeof UserMenu>

export const Default: StoryObj<typeof UserMenu> = {}
