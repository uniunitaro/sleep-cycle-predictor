import { Meta, StoryObj } from '@storybook/react'
import SignedInHeader from './SignedInHeader'

export default { component: SignedInHeader } satisfies Meta<
  typeof SignedInHeader
>

export const Default: StoryObj<typeof SignedInHeader> = {}
